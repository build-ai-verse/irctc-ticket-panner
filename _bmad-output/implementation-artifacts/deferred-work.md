- source_spec: none
  summary: Booking-day reminder email capture (Story-6 / FR-6) — opt-in email form + serverless storage endpoint (Vercel KV or form provider).
  evidence: Split from the initial IRCTC Ticket Planner build as an independently shippable deliverable. The calculator site launches without it; it adds no user value until reminder sending is also built (itself out of v1 scope). Revisit right after the calculator site ships.
